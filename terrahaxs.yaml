version: 3
automerge: true
defaults:
  workflow: myworkflow
  roles:
    aws: arn:aws:iam::530084066544:role/terrahaxs-worker
projects:
- dir: gh_dir.*
  apply_requirements: [mergeable, approved]
workflows:
  myworkflow:
    plan:
      steps:
      - init
      - plan
    apply:
      steps:
      - init
      - apply
